version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12.6
    steps:
      - checkout
      - run: go test -count=1 -timeout=3m -v ./...

workflows:
  version: 2
  build:
    jobs:
      - build
