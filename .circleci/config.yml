version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12.6
    steps:
      - checkout
      - run: go test ./... -test.v -test.timeout 3m

workflows:
  version: 2
  build:
    jobs:
      - build
