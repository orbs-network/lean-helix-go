package e2e

import (
	"testing"
)

// Adapted from PBFT.spec.ts

func TestSendPreprepareOnlyIfLeader(t *testing.T) {
	//network := CreateTestNetwork()
	//h := newHarness()
	//s := NewLeanHelixService(blockUtils, keyManager, networkCommunication)
	//s.start()
	//h.networkCommunication.When("sendToMembers", &services.CommitBlockInput{expectedBlockPair}).Return(nil, nil).Times(1)
	//h.gossip.When("SendBenchmarkConsensusCommitted", mock.AnyIf(fmt.Sprintf("LastCommittedBlockHeight equals %d, recipient equals %s and sender equals %s", expectedLastCommitted, expectedRecipient, expectedSender), lastCommittedReplyMatcher)).Times(1)
	//s.stop()
}
